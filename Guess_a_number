import random

a = random.randint(1,9)
guess = int(input("Guess a number between 1 to 9: "))

def guess(a, guess):
    if a == guess:
        return("You got it")
    else:
        return("try again")

print(guess)
